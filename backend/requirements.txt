# Ortholingo backend — Python 3.11+, ffmpeg on PATH
# setup: uv venv --python 3.11 .venv && uv pip install -r requirements.txt
fastapi>=0.115
uvicorn>=0.30
faster-whisper>=1.1
python-multipart>=0.0.9
